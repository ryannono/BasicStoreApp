{
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.0.0"
  },
  "scripts": {
    "start": "npm install --omit=dev && cd server && npm install --omit=dev && node build/src/server.js && cd .."
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "dotenv": "^16.3.1",
    "stripe": "^12.14.0"
  }
}
